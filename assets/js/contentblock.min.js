var module_80992206946 = (function () {
  var __hs_messages = {};
  (i18n_getmessage = function () {
    return hs_i18n_getMessage(__hs_messages, hsVars.language, arguments);
  }),
    (i18n_getlanguage = function () {
      return hsVars.language;
    }),
    [
      ...document.querySelectorAll(
        ".csol-cta-block .csol-cta-block-image img[data-src-random]"
      ),
    ].forEach((t) =>
      ((t) => {
        const { srcRandom: e } = t.dataset || {},
          a = t.parentElement;
        if (!e.length || t.getAttribute("src")) return;
        const r = e.split(","),
          o = Math.floor(Math.random() * r.length),
          c = t.cloneNode(!1);
        c.removeAttribute("data-src-random"),
          c.setAttribute("data-src", r[o]),
          (a.innerHTML = ""),
          a.appendChild(c);
      })(t)
    );
})();
